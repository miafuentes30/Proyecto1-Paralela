#pragma once

namespace fruitcat {

// Draws the first terrain layer: a light-reactive green platform.
// Later this same plane will receive UV coordinates and a grass texture.
void drawTerrain(float halfWidth, float halfDepth, float height);

} // namespace fruitcat
